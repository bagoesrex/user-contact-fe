export interface Contact {
    id: number
    first_name: string
    last_name: string
    email: string
    phone: string
}

export interface ContactInput {
    first_name: string
    last_name: string
    email: string
    phone: string
}